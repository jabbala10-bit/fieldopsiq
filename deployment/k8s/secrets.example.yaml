# Template only — do not commit real values. Generate with:
#   kubectl create secret generic fieldopsiq-secrets \
#     --from-literal=api-auth-token=<real-token> \
#     --from-literal=central-server-api-key=<real-key>

apiVersion: v1
kind: Secret
metadata:
  name: fieldopsiq-secrets
type: Opaque
stringData:
  api-auth-token: "REPLACE_ME"
  central-server-api-key: "REPLACE_ME"
